apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "oproxy.fullname" . }}
  labels:
    app: {{ include "oproxy.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "oproxy.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "oproxy.name" . }}
    spec:
      containers:
        - name: oproxy
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          resources:
{{ toYaml .Values.resources | indent 12 }}
