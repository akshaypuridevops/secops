apiVersion: v1
kind: Service
metadata:
  name: {{ include "elastalert2.fullname" . }}
  labels:
    {{- include "elastalert2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "elastalert2.selectorLabels" . | nindent 4 }}
